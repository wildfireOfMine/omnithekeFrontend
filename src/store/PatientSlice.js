import { createSlice } from "@reduxjs/toolkit";



const initialState = {
    session: []
}


export const patientSlice = createSlice({
    name: "patient",
    initialState,
    reducers: {
        
    }
})


export const {

} = patientSlice.actions;

