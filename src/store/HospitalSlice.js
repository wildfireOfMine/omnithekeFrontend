import { createSlice } from "@reduxjs/toolkit";



const initialState = {
    session: []
}


export const hospitalSlice = createSlice({
    name: "hospital",
    initialState,
    reducers: {
        
    }
})


export const {

} = hospitalSlice.actions;

