import { createSlice } from "@reduxjs/toolkit";



const initialState = {
    session: []
}


export const doctorSlice = createSlice({
    name: "doctor",
    initialState,
    reducers: {
        
    }
})


export const {

} = doctorSlice.actions;
