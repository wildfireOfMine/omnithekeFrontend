import { createSlice } from "@reduxjs/toolkit";



const initialState = {
    session: []
}


export const adminSlice = createSlice({
    name: "admin",
    initialState,
    reducers: {
        
    }
})


export const {

} = adminSlice.actions;

